import type { DiscordRouteContext } from "../context.js";

export function applicationCommandsRoutes(_ctx: DiscordRouteContext): void {
  // implemented by the application-commands route agent (P2)
}
